node_modules/
.env
.env.*.bak
*.local.bak
*.bak
*.log
.DS_Store
dist/
build/
*.xlsx
*.xls
temp/

# Генерируется scripts/generatePositionSql.js
position.sql

# Next.js build output (собирается через npm run build в backend/ и frontend_client/)
backend/.next/
frontend_client/.next/

# Локальные секреты и env Next
**/.env.local

# Резервные дампы БД (не нужны для сборки; схема — migrations/)
exports/

# Локальные бэкапы и патчи (не коммитить)
exports_bak_*/
exports_local_backup_*/
local_before_test_pstock.patch
"new elements"/

# Устаревший дубликат Next (раньше submodule); при необходимости — отдельный репозиторий
frontend/

